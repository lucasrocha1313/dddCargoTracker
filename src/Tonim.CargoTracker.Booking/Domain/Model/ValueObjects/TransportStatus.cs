namespace Tonim.CargoTracker.Booking.Domain.Model.ValueObjects
{
    public enum TransportStatus
    {
        NOT_RECEIVED,
        IN_PORT,
        ONBOARD_CARRIER,
        CLAIMED,
        UNKNOWN
    }
}